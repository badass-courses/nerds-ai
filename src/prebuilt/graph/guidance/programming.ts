import { GraphGuidance } from "../index.js";

export const programming_graph_guidance: GraphGuidance = {
  vertex_labels: ["ProgrammingLanguage", "Framework", "Technique", "Keyword", "Strategy", "Tip", "Problem", "Class", "Type", "Pattern"],
  edge_labels: ["solves", "causes", "prevents", "improves", "implements", "takes_type", "returns_type"]
}