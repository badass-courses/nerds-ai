import { GraphGuidance } from "../index.js";

export const programming_graph_guidance: GraphGuidance = {
  vertex_labels: ["ProgrammingLanguage", "Framework", "Technique", "Strategy", "Tip", "Problem", "Class", "Type", "Pattern"],
  edge_labels: ["uses", "solves", "is_a", "is_part_of", "causes", "prevents", "improves", "implements"]
}